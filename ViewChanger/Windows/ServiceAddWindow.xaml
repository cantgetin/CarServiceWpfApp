<Window x:Class="ViewChanger.Windows.ServiceAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ViewChanger.Windows"
        mc:Ignorable="d"
        Title="ServiceAddWindow" Height="300" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <Label Margin="5,0,0,0">Название услуги:</Label>
            <TextBox x:Name="textBoxTitle" Margin="10,0,10,0" Height="22"/>
            <Label Margin="5,0,0,0">Цена услуги:</Label>
            <TextBox x:Name="textBoxPrice" Margin="10,0,10,0" Height="22"/>
            <Label Margin="5,0,0,0">Время необходимое для выполнения услуги, часов:</Label>
            <TextBox x:Name="textBoxHours" Margin="10,0,10,0" Height="22"/>
        </StackPanel>
        <Button x:Name="button1" Height="30" Content="Принять" Click="Btn_Accept" HorizontalAlignment="Right" Width="150" VerticalAlignment="Bottom" Grid.Column="1" Margin="0,0,10,10"/>
    </Grid>
</Window>
